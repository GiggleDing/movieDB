@model IEnumerable<MvcMovie.Models.Attention>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<p>
    <a asp-action="AttentionClick">Attention</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AttentionID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AttentionID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.AttentionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.AttentionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.AttentionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
